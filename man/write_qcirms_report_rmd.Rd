% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reports.R
\name{write_qcirms_report_rmd}
\alias{write_qcirms_report_rmd}
\title{Write a QCIRMS report template as an R Markdown file}
\usage{
write_qcirms_report_rmd(
  DATA_PATH,
  REF_TIME_PATH,
  PLOTS_PATH,
  RESULT_PATH,
  output_rmd = file.path(RESULT_PATH, "QCIRMS_report.Rmd"),
  report_title = "QCIRMS Processing Report",
  ref_file = "referencePeaks_expectedTimes.txt",
  check_internal_standards = TRUE,
  output_formats = c("html", "pdf"),
  overwrite = TRUE
)
}
\arguments{
\item{DATA_PATH}{Character string giving the directory containing input
\code{.dxf} files.}

\item{REF_TIME_PATH}{Character string giving the directory containing the
expected reference-times file.}

\item{PLOTS_PATH}{Character string giving the directory where plot files
created by the report should be written.}

\item{RESULT_PATH}{Character string giving the directory where result files
and the generated \code{.Rmd} should be written.}

\item{output_rmd}{Character string giving the path of the output \code{.Rmd} file.
Defaults to \code{file.path(RESULT_PATH, "QCIRMS_report.Rmd")}.}

\item{report_title}{Character string giving the report title.}

\item{ref_file}{Character string giving the expected reference-times filename.
Defaults to \code{"referencePeaks_expectedTimes.txt"}.}

\item{check_internal_standards}{Logical; whether the generated report should
run \code{QAQC_IRMS()} with internal-standard checking enabled.}

\item{output_formats}{Character vector of output formats to include in the
YAML header. Supported values are \code{"html"} and \code{"pdf"}.}

\item{overwrite}{Logical; whether to overwrite an existing \code{.Rmd} file.}
}
\value{
Invisibly returns the path to the generated \code{.Rmd} file.
}
\description{
Creates an \code{.Rmd} report template based on the QCIRMS batch-processing
workflow. The generated report can be knitted to HTML or PDF and is designed
to both write output files to disk and embed key tables, summaries, and plots
directly in the report.
}
\details{
The report includes:
\itemize{
\item path summary
\item DXF file discovery
\item single-file preview
\item reference-time table
\item batch raw-data summary
\item batch plotting
\item full \code{QAQC_IRMS()} execution
\item QA/QC output summaries
\item optional internal-standard summaries
\item session information
}

The generated report is parameterized through YAML \code{params}, but the file is
written with the provided path values already filled in. Users can knit it
directly in RStudio or render it programmatically with \code{rmarkdown::render()}.

The report writes results to \code{PLOTS_PATH} and \code{RESULT_PATH}, while also
embedding selected previews and summary tables in the knitted document.
}
\examples{
\dontrun{
report_file <- write_qcirms_report_rmd(
  DATA_PATH = "inst/extdata/dxf_files/abiotic",
  REF_TIME_PATH = "inst/extdata/qc",
  PLOTS_PATH = "plots",
  RESULT_PATH = "results"
)

rmarkdown::render(report_file, output_format = "html_document")
}

}
