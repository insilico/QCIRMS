% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reports.R
\name{render_qcirms_report}
\alias{render_qcirms_report}
\title{Write and render a QCIRMS report}
\usage{
render_qcirms_report(
  DATA_PATH,
  REF_TIME_PATH,
  PLOTS_PATH,
  RESULT_PATH,
  output_format = c("html_document", "pdf_document"),
  output_file = NULL,
  report_rmd = file.path(RESULT_PATH, "QCIRMS_report.Rmd"),
  report_title = "QCIRMS Processing Report",
  ref_file = "referencePeaks_expectedTimes.txt",
  check_internal_standards = TRUE,
  overwrite = TRUE,
  clean = TRUE,
  envir = new.env(parent = globalenv())
)
}
\arguments{
\item{DATA_PATH}{Character string giving the directory containing input
\code{.dxf} files.}

\item{REF_TIME_PATH}{Character string giving the directory containing the
expected reference-times file.}

\item{PLOTS_PATH}{Character string giving the directory where plot files
created by the report should be written.}

\item{RESULT_PATH}{Character string giving the directory where result files,
the generated \code{.Rmd}, and the rendered report should be written.}

\item{output_format}{Character string giving the render format. Supported
values are \code{"html_document"} and \code{"pdf_document"}.}

\item{output_file}{Optional character string giving the rendered report
filename. If \code{NULL}, a default is chosen based on \code{output_format}.}

\item{report_rmd}{Optional character string giving the \code{.Rmd} filename to
write. Defaults to \code{file.path(RESULT_PATH, "QCIRMS_report.Rmd")}.}

\item{report_title}{Character string giving the report title.}

\item{ref_file}{Character string giving the expected reference-times filename.}

\item{check_internal_standards}{Logical; whether the report should run
\code{QAQC_IRMS()} with internal-standard checking enabled.}

\item{overwrite}{Logical; whether to overwrite an existing \code{.Rmd} file.}

\item{clean}{Logical; passed to \code{\link[rmarkdown:render]{rmarkdown::render()}}. If \code{TRUE}, intermediate
files produced during rendering are cleaned up.}

\item{envir}{Environment in which to render the report. Defaults to
\code{new.env(parent = globalenv())}.}
}
\value{
Invisibly returns a list with elements:
\itemize{
\item \code{rmd_file}: path to the written \code{.Rmd}
\item \code{rendered_file}: path to the rendered output
}
}
\description{
Convenience wrapper around \code{\link[=write_qcirms_report_rmd]{write_qcirms_report_rmd()}} and
\code{\link[rmarkdown:render]{rmarkdown::render()}}. This function writes a parameterized QCIRMS report
template and immediately renders it to HTML or PDF.
}
\examples{
\dontrun{
out <- render_qcirms_report(
  DATA_PATH = "inst/extdata/dxf_files/abiotic",
  REF_TIME_PATH = "inst/extdata/qc",
  PLOTS_PATH = "plots",
  RESULT_PATH = "results",
  output_format = "html_document"
)

out$rendered_file
}

}
